from django.contrib import admin

# Register your models here.
# from logcat.models import System_info, Host, Processes

# admin.site.register(System_info)
# admin.site.register(Host)
# admin.site.register(Processes)
